apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: axion
  namespace: argocd
spec:
  description: Axion platform
  # Allow the Axion monorepo (tighten with explicit lists for prod)
  sourceRepos:
    - "https://github.com/santandevops01012025/Axion-UI"
    - "https://github.com/santandevops01012025/Axion-UI.git"
  destinations:
    - namespace: axion
      server: https://kubernetes.default.svc
    - namespace: argocd
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: ""
      kind: Namespace
    - group: ""
      kind: Secret
    - group: ""
      kind: PersistentVolumeClaim
  roles:
    - name: read-only
      description: Read access for auditors
      policies:
        - p, proj:axion:read-only, applications, get, axion/*, allow
      groups:
        - axion-auditors