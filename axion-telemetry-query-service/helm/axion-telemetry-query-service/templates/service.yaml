apiVersion: v1
kind: Service
metadata:
  name: {{ include "tq.fullname" . }}
  labels:
    {{- include "tq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tq.selectorLabels" . | nindent 4 }}