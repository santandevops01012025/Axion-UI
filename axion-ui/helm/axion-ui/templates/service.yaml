apiVersion: v1
kind: Service
metadata:
  name: {{ include "axion-ui.fullname" . }}
  labels:
    {{- include "axion-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "axion-ui.selectorLabels" . | nindent 4 }}